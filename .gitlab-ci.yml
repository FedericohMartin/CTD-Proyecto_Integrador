stages:
  - triggers

trigger_front:
  stage: triggers
  trigger:
    include: front/booking/.gitlab-ci.yml
  rules:
    - changes:
      - front/**/*


trigger_back:
  stage: triggers
  trigger:
    include: back/digitalbooking-rentalcars/.gitlab-ci.yml
  rules:
    - changes:
      - back/**/*